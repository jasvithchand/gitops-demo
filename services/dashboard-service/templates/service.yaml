apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: services
  labels:
    app: dashboard-service
spec:
  selector:
    app: dashboard-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      name: http
