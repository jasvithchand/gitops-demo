apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: services
  labels:
    app: analytics-service
spec:
  selector:
    app: analytics-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3004
      name: http
